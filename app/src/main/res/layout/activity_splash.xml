<?xml version="1.0" encoding="utf-8"?><!-- Auto layout, variables, and unit scale are not yet supported -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToOutline="true"
    android:layout_gravity="center"
    android:gravity="center"
    android:background="#DDE6ED"
    android:orientation="vertical"
    tools:context=".SplashActivity">
    <!-- Auto layout, variables, and unit scale are not yet supported -->
    <View
        android:id="@+id/group_44"
        android:layout_width="169.59dp"
        android:layout_height="130.59dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/workwiselogo" />

    <!-- Auto layout, variables, and unit scale are not yet supported -->
    <TextView
        android:id="@+id/workwise"
        android:layout_width="258dp"
        android:layout_height="82dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="@string/workwise"
        android:textAppearance="@style/workwise"
        android:lineSpacingExtra="-8sp"
        android:gravity="center_horizontal|center_vertical" />

</LinearLayout>